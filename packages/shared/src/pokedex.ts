// GEN3_MAX_NATIONAL_DEX_NUMBER stores the highest National Dex number available to the current Gen 3 save parser.
// Gen 3 parser modules import this so parser limits stay explicit even as the app-wide species catalog grows.
export const GEN3_MAX_NATIONAL_DEX_NUMBER = 386;

// SUPPORTED_POKEDEX_MAX_NATIONAL_DEX_NUMBER stores the highest canonical species row currently seeded in the app.
// Seed and dex-expansion work import this so app-wide species support can grow independently from parser support.
export const SUPPORTED_POKEDEX_MAX_NATIONAL_DEX_NUMBER = 493;
